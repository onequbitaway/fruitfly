# Desktop clip for X

Use the full 166,700-cell model. Keep the current app and public repo unchanged for this preview. Render a clean desktop scene with Notes and Projects windows, a menu bar, and a dock. These windows are staged. The actual model drives the readouts and feeding. Guided movement remains a pet rule.

Palette: sky #B9CFF0, ice #DEE8F9, paper #FBFCFE, ink #29394F, muted #64768D, crumb #F4B45F. Use the Mac system font for the desktop UI and a medium-weight 43-point headline. Keep the fly large enough to see in a social feed.

Layout: title above two overlapping windows. The fly crosses their edges from left to right. A cursor drops one small crumb on the right window. A compact strip shows actual active cells and feeding output. Put the source/scene label along the bottom. Avoid decorative neuron flashes.

Review before building: the desktop, window edges, pointer, and fly carry the idea. Do not add a large dashboard or a separate set of cards. Keep the visual focus on the fly. A second export can cut to the already verified full-map clip.

Export 1280 × 800 H.264 MP4 at 30 fps and a 10 fps GIF. Use the same calculated path for both. Store the cell values and trial states. Never alter brain rates for a visual effect.

## Final review

Checked stills during the crossing and landing, then decoded a frame from the H.264 export. Fixed an uncovered corner of the wallpaper gradient. Drew the crumb relative to its initial portion so it remains visible in a small feed. The displayed MN9 rate and food use remain unchanged.

The full model finished the 0.1 portion at 6.4 seconds. The remainder of the 12-second desktop clip shows the fly resume exploring. The combined 20-second clip then cuts to the earlier, separate full-map recording; its clock starts again. Both video files are H.264, 1280 × 800, 30 fps. The looping GIF is 960 × 600 at 10 fps.

All 20,004,000 rates, class means, model times, and food-use steps passed the saved-value check. The file checks verified video duration, frame count, codec, and GIF frame timing.
